cubes:
  - name: dim_products
    sql_table: fedex_dwh_common.dim_products
    data_source: default
    title: Products

    joins: []

    dimensions:
      - name: id
        sql: id
        type: string
        primary_key: true

      - name: product_code
        sql: product_code
        type: string

      - name: product_name
        sql: product_name
        type: string

      - name: style
        sql: style
        type: string

      - name: category
        sql: category
        type: string

      - name: manufacturer
        sql: manufacturer
        type: string

      - name: supplier
        sql: supplier
        type: string

    measures:
      - name: count
        type: count

    pre_aggregations:
      # Pre-aggregation definitions go here.
      # Learn more in the documentation: https://cube.dev/docs/caching/pre-aggregations/getting-started

