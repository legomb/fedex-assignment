cubes:
  - name: dim_stores
    sql_table: fedex_dwh_common.dim_stores
    data_source: default
    title: Stores

    joins: []

    dimensions:
      - name: id
        sql: id
        type: string
        primary_key: true

      - name: store_code
        sql: store_code
        type: string

      - name: store_name
        sql: store_name
        type: string

      - name: city
        sql: city
        type: string

      - name: store_address
        sql: store_address
        type: string

      - name: phone_number
        sql: phone_number
        type: string

    measures:
      - name: count
        type: count

    segments:
      - name: sf_stores
        sql: "{CUBE}.city = 'City4'"

    pre_aggregations:
      # Pre-aggregation definitions go here.
      # Learn more in the documentation: https://cube.dev/docs/caching/pre-aggregations/getting-started

